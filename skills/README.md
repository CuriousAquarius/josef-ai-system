These are Josef's custom Claude Code skills backed up from `~/.claude/skills/`.

Symlinked skills (third-party bundles via `.agents`) are not included — reinstall those separately via `npx clawhub install <skill>` or the original install commands.
